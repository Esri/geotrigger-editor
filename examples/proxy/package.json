{
  "name": "geotrigger-editor-proxy",
  "version": "0.0.1",
  "description": "Geotrigger Editor proxy for browsers that don't support CORS",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Esri Portland R&D Center <portland@esri.com>",
  "license": "Apache 2.0",
  "dependencies": {
    "request": "~2.27.0",
    "metaserve": "~0.1.0"
  }
}
